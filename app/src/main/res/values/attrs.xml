<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Common Attributes -->
    <attr name="radius" format="dimension" />
    <attr name="topLeftRadius" format="dimension" />
    <attr name="topRightRadius" format="dimension" />
    <attr name="bottomLeftRadius" format="dimension" />
    <attr name="bottomRightRadius" format="dimension" />

    <!-- CodiaRoundImageView Attributes -->
    <declare-styleable name="CodiaRoundImageView">
        <attr name="radius" />
        <attr name="topLeftRadius" />
        <attr name="topRightRadius" />
        <attr name="bottomLeftRadius" />
        <attr name="bottomRightRadius" />
    </declare-styleable>

    <!-- CodiaRoundConstraintLayout (แทนที่ FlexboxLayout) -->
    <declare-styleable name="CodiaRoundConstraintLayout">
        <attr name="radius" />
        <attr name="topLeftRadius" />
        <attr name="topRightRadius" />
        <attr name="bottomLeftRadius" />
        <attr name="bottomRightRadius" />
    </declare-styleable>

    <!-- CodiaRoundFrameLayout Attributes -->
    <declare-styleable name="CodiaRoundFrameLayout">
        <attr name="widthHeightRadio" format="dimension" />
        <attr name="cornerRadius" format="dimension" />
        <attr name="topLeftRadius" />
        <attr name="topRightRadius" />
        <attr name="bottomRightRadius" />
        <attr name="bottomLeftRadius" />
    </declare-styleable>

</resources>
